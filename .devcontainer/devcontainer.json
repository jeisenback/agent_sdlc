{
  "name": "agent_sdlc",
  "image": "mcr.microsoft.com/devcontainers/python:0-3.11",
  "features": {},
  "forwardPorts": [],
  "postCreateCommand": "python -m pip install --upgrade pip setuptools wheel && if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi",
  "remoteUser": "vscode",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-azuretools.vscode-docker",
        "ms-vscode-remote.remote-containers"
      ]
    }
  }
}
